import 'package:flutter/material.dart';

class AppColors {
  static const Color lime = Color(0xFFD6FF79);
  static const Color lightGreen = Color(0xFFB0FF92);
  static const Color background = Color(0xFFFFF2F1);
  static const Color lightPurple = Color(0xFFA09BE7);
  static const Color primary = Color(0xFF5F00BA);
  
  static const Color accent = lightPurple;
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF666666);
}

class AppConstants {
  static const double defaultPadding = 20.0;
  static const double defaultRadius = 12.0;
  
  static const Duration splashDuration = Duration(seconds: 3);
  static const Duration pageTransitionDuration = Duration(milliseconds: 300);
}